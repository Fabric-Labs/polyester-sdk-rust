// @generated by connectrpc-codegen. DO NOT EDIT.

///Shorthand for `OwnedView<GetRateLimitConfigRequestView<'static>>`.
pub type OwnedGetRateLimitConfigRequestView = ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigRequestView<'static>,
>;
///Shorthand for `OwnedView<GetRateLimitConfigResponseView<'static>>`.
pub type OwnedGetRateLimitConfigResponseView = ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigResponseView<'static>,
>;
///Shorthand for `OwnedView<GetTradingRateLimitsRequestView<'static>>`.
pub type OwnedGetTradingRateLimitsRequestView = ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsRequestView<'static>,
>;
///Shorthand for `OwnedView<GetTradingRateLimitsResponseView<'static>>`.
pub type OwnedGetTradingRateLimitsResponseView = ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsResponseView<'static>,
>;
impl ::connectrpc::Encodable<crate::proto::ratelimit::v1::GetRateLimitConfigResponse>
for crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::ratelimit::v1::GetRateLimitConfigResponse>
for ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::ratelimit::v1::GetTradingRateLimitsResponse>
for crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::ratelimit::v1::GetTradingRateLimitsResponse>
for ::buffa::view::OwnedView<
    crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
}
/// Full service name for this service.
pub const RATE_LIMIT_SERVICE_SERVICE_NAME: &str = "ratelimit.v1.RateLimitService";
/// Static [`Spec`](::connectrpc::Spec) for the server-side `GetRateLimitConfig` RPC.
///
/// The dispatcher surfaces this on
/// [`RequestContext::spec`](::connectrpc::RequestContext::spec).
pub const RATE_LIMIT_SERVICE_GET_RATE_LIMIT_CONFIG_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/ratelimit.v1.RateLimitService/GetRateLimitConfig",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the server-side `GetTradingRateLimits` RPC.
///
/// The dispatcher surfaces this on
/// [`RequestContext::spec`](::connectrpc::RequestContext::spec).
pub const RATE_LIMIT_SERVICE_GET_TRADING_RATE_LIMITS_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/ratelimit.v1.RateLimitService/GetTradingRateLimits",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// RateLimitService serves public trading quota reference data and authenticated
/// effective trading limits.
///
/// # Implementing handlers
///
/// Implement methods with plain `async fn`; the returned future satisfies
/// the `Send` bound automatically.
///
/// **Unary and server-streaming requests** arrive as
/// [`ServiceRequest<'_, Req>`](::connectrpc::ServiceRequest): a zero-copy
/// view of the request plus its body, valid for the duration of the call.
/// Fields are read directly (`request.name` is a `&str` into the decoded
/// buffer) and the borrow may be held across `.await` points. Anything
/// that must outlive the call — `tokio::spawn`, channels, server state,
/// or data captured by a returned response stream — takes owned data:
/// call `request.to_owned_message()` (or copy the specific fields)
/// first.
///
/// **Client-streaming and bidi requests** arrive as
/// [`InboundStream<Req>`](::connectrpc::InboundStream) — a
/// `ServiceStream` of [`StreamMessage`](::connectrpc::StreamMessage)s.
/// Each item owns its decoded buffer and is `Send + 'static`, so items
/// can be buffered or moved into spawned tasks; read fields zero-copy
/// through the generated accessor methods (`item.name()`) or `.view()`,
/// convert with `.to_owned_message()`, or yield an item back unchanged —
/// `StreamMessage<M>` implements `Encodable<M>`.
///
/// Request types resolved through `extern_path` (e.g. well-known types
/// from another crate) use the same wrappers; the crate that owns the
/// type must be generated with buffa ≥ 0.8.0 and views enabled so the
/// backing `HasMessageView` impl exists.
///
/// The `impl Encodable<Out>` return bound accepts the owned `Out`, the
/// generated `OutView<'_>` / `OwnedOutView`,
/// [`MaybeBorrowed`](::connectrpc::MaybeBorrowed), or
/// [`PreEncoded`](::connectrpc::PreEncoded) for handlers that encode a
/// non-`'static` view internally and pass the bytes across the handler
/// boundary. View bodies are not emitted for output types mapped via
/// `extern_path` (the impl would be an orphan); return owned for
/// WKT/extern outputs.
///
/// Server-streaming and bidi-streaming methods return
/// `ServiceStream<impl Encodable<Out> + Send + use<Self>>`. The
/// `use<Self>` precise-capturing clause excludes `&self`'s lifetime and
/// the request's lifetime (unary methods use `use<'a, Self>` and may
/// borrow from `&self`), so stream items must be `'static` and cannot
/// borrow from the request. To stream view-encoded data, encode each
/// item inside the stream body and yield
/// [`PreEncoded`](::connectrpc::PreEncoded) — see its `# Streaming
/// example` doc.
#[allow(clippy::type_complexity)]
pub trait RateLimitService: Send + Sync + 'static {
    /// Get the complete active placement and cancellation quota catalog for
    /// VIP0 through VIP10.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn get_rate_limit_config<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::ratelimit::v1::GetRateLimitConfigResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Get the effective placement and cancellation limits for the authenticated
    /// caller's root account or selected subaccount.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn get_trading_rate_limits<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::ratelimit::v1::GetTradingRateLimitsResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
}
/// Extension trait for registering a service implementation with a Router.
///
/// This trait is automatically implemented for all types that implement the service trait.
/// Prefer [`Router::add_service`](::connectrpc::Router::add_service) for
/// top-down registration; `register` remains available for compatibility
/// and cases where the service-first call shape is more convenient.
///
/// # Example
///
/// ```rust,ignore
/// use std::sync::Arc;
///
/// let service = Arc::new(MyServiceImpl);
/// let router = service.register(Router::new());
/// ```
pub trait RateLimitServiceExt: RateLimitService {
    /// Register this service implementation with a Router.
    ///
    /// Takes ownership of the `Arc<Self>` and returns a new Router with
    /// this service's methods registered.
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router;
}
impl<S: RateLimitService> RateLimitServiceExt for S {
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router {
        router
            .route_view(
                RATE_LIMIT_SERVICE_SERVICE_NAME,
                "GetRateLimitConfig",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.get_rate_limit_config(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::ratelimit::v1::GetRateLimitConfigResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(RATE_LIMIT_SERVICE_GET_RATE_LIMIT_CONFIG_SPEC)
            .route_view(
                RATE_LIMIT_SERVICE_SERVICE_NAME,
                "GetTradingRateLimits",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.get_trading_rate_limits(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::ratelimit::v1::GetTradingRateLimitsResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(RATE_LIMIT_SERVICE_GET_TRADING_RATE_LIMITS_SPEC)
    }
}
/// Type-inference marker used by [`Router::add_service`](::connectrpc::Router::add_service).
#[doc(hidden)]
pub struct RateLimitServiceRegisterMarker;
impl<S: RateLimitService> ::connectrpc::ServiceRegister<RateLimitServiceRegisterMarker>
for ::std::sync::Arc<S> {
    fn register_service(self, router: ::connectrpc::Router) -> ::connectrpc::Router {
        <S as RateLimitServiceExt>::register(self, router)
    }
}
/// Monomorphic dispatcher for `RateLimitService`.
///
/// Unlike `.register(Router)` which type-erases each method into an `Arc<dyn ErasedHandler>` stored in a `HashMap`, this struct dispatches via a compile-time `match` on method name: no vtable, no hash lookup.
///
/// # Example
///
/// ```rust,ignore
/// use connectrpc::ConnectRpcService;
///
/// let server = RateLimitServiceServer::new(MyImpl);
/// let service = ConnectRpcService::new(server);
/// // hand `service` to axum/hyper as a fallback_service
/// ```
pub struct RateLimitServiceServer<T> {
    inner: ::std::sync::Arc<T>,
}
impl<T: RateLimitService> RateLimitServiceServer<T> {
    /// Wrap a service implementation in a monomorphic dispatcher.
    pub fn new(service: T) -> Self {
        Self {
            inner: ::std::sync::Arc::new(service),
        }
    }
    /// Wrap an already-`Arc`'d service implementation.
    pub fn from_arc(inner: ::std::sync::Arc<T>) -> Self {
        Self { inner }
    }
}
impl<T> Clone for RateLimitServiceServer<T> {
    fn clone(&self) -> Self {
        Self {
            inner: ::std::sync::Arc::clone(&self.inner),
        }
    }
}
impl<T: RateLimitService> ::connectrpc::Dispatcher for RateLimitServiceServer<T> {
    #[inline]
    fn lookup(
        &self,
        path: &str,
    ) -> Option<::connectrpc::dispatcher::codegen::MethodDescriptor> {
        let method = path.strip_prefix("ratelimit.v1.RateLimitService/")?;
        match method {
            "GetRateLimitConfig" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(RATE_LIMIT_SERVICE_GET_RATE_LIMIT_CONFIG_SPEC),
                )
            }
            "GetTradingRateLimits" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(RATE_LIMIT_SERVICE_GET_TRADING_RATE_LIMITS_SPEC),
                )
            }
            _ => None,
        }
    }
    fn call_unary(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::Payload,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("ratelimit.v1.RateLimitService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            "GetRateLimitConfig" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
                    >::from_parts(&req, &body);
                    svc.get_rate_limit_config(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::ratelimit::v1::GetRateLimitConfigResponse,
                        >(format)
                })
            }
            "GetTradingRateLimits" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
                    >::from_parts(&req, &body);
                    svc.get_trading_rate_limits(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::ratelimit::v1::GetTradingRateLimitsResponse,
                        >(format)
                })
            }
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_server_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::buffa::bytes::Bytes,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("ratelimit.v1.RateLimitService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
    fn call_client_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("ratelimit.v1.RateLimitService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_bidi_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("ratelimit.v1.RateLimitService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
}
/// Client for this service.
///
/// Generic over `T: ClientTransport`. For **gRPC** (HTTP/2), use
/// `Http2Connection` — it has honest `poll_ready` and composes with
/// `tower::balance` for multi-connection load balancing. For **Connect
/// over HTTP/1.1** (or unknown protocol), use `HttpClient`.
///
/// # Working with the response
///
/// Unary calls return [`UnaryResponse<OwnedView<FooView>>`](::connectrpc::client::UnaryResponse).
/// [`view()`](::connectrpc::client::UnaryResponse::view) borrows the response
/// message, so field access is zero-copy:
///
/// ```rust,ignore
/// let resp = client.get_rate_limit_config(request).await?;
/// let name: &str = resp.view().name;  // borrow into the response buffer
/// ```
///
/// If you need the owned struct (e.g. to store or pass by value), use
/// [`into_owned()`](::connectrpc::client::UnaryResponse::into_owned):
///
/// ```rust,ignore
/// let owned = client.get_rate_limit_config(request).await?.into_owned();
/// ```
///
/// [`into_view()`](::connectrpc::client::UnaryResponse::into_view) keeps the
/// zero-copy decoded body (an `OwnedView`) without copying; field access on it
/// goes through `.reborrow()`. Streaming responses yield one
/// [`StreamMessage`](::connectrpc::StreamMessage) per received message from
/// `.message().await` — read fields zero-copy through the generated accessor
/// methods (`msg.name()`) or `.view()`, or convert with `.to_owned_message()`.
#[derive(Clone)]
pub struct RateLimitServiceClient<T> {
    transport: T,
    config: ::connectrpc::client::ClientConfig,
}
impl<T> RateLimitServiceClient<T>
where
    T: ::connectrpc::client::ClientTransport,
    <T::ResponseBody as ::connectrpc::http_body::Body>::Error: ::std::fmt::Display,
{
    /// Create a new client with the given transport and configuration.
    pub fn new(transport: T, config: ::connectrpc::client::ClientConfig) -> Self {
        Self { transport, config }
    }
    /// Get the client configuration.
    pub fn config(&self) -> &::connectrpc::client::ClientConfig {
        &self.config
    }
    /// Get a mutable reference to the client configuration.
    pub fn config_mut(&mut self) -> &mut ::connectrpc::client::ClientConfig {
        &mut self.config
    }
    /// Call the GetRateLimitConfig RPC. Sends a request to /ratelimit.v1.RateLimitService/GetRateLimitConfig.
    pub async fn get_rate_limit_config(
        &self,
        request: crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.get_rate_limit_config_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the GetRateLimitConfig RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn get_rate_limit_config_with_options(
        &self,
        request: crate::proto::ratelimit::v1::GetRateLimitConfigRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::ratelimit::v1::__buffa::view::GetRateLimitConfigResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                RATE_LIMIT_SERVICE_SERVICE_NAME,
                "GetRateLimitConfig",
                request,
                options,
            )
            .await
    }
    /// Call the GetTradingRateLimits RPC. Sends a request to /ratelimit.v1.RateLimitService/GetTradingRateLimits.
    pub async fn get_trading_rate_limits(
        &self,
        request: crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.get_trading_rate_limits_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the GetTradingRateLimits RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn get_trading_rate_limits_with_options(
        &self,
        request: crate::proto::ratelimit::v1::GetTradingRateLimitsRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::ratelimit::v1::__buffa::view::GetTradingRateLimitsResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                RATE_LIMIT_SERVICE_SERVICE_NAME,
                "GetTradingRateLimits",
                request,
                options,
            )
            .await
    }
}
